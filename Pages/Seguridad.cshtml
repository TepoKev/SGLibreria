@page
@model IndexModel
@{
ViewData["Title"] = "Seguridad";
}

<form class="px-5">
    <div class="container-fluid border border-secondary-100 rounded pb-4 px-5" data-spy="scroll" data-target="#myScrollspy" data-offset="1">
        <div class="row">
            <div class="form-control-plaintext text-center" style="font-size: 25px">
                <p>Realizar copia de información</p>
            </div>
        </div>
        <div class="btn-group offset-2">
            <button type="button" class="btn btn-outline-success btn-lg"><i class="fas fa-cloud-download-alt"></i> Crear copia de seguridad</button>
            <button type="button" class="btn btn-outline-primary btn-lg"><i class="fas fa-cloud-upload-alt"></i> Restaurar copia de seguridad</button>
        </div>
    </div>
</form>